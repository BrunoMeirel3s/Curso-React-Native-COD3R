import { StyleSheet } from 'react-native';

/**
 * A maneira de estilização dos componentes é levemente diferente, sendo necessário utilizarmos o
 * componente StyleSheet importado de reac-native, desta forma podemos criar 'classes' que serão utilizadas
 * nos componentes a serem personalizados
 */
export default StyleSheet.create({
  txtG: {
    fontSize: 32,
    textAlign: "center"
  },
});
